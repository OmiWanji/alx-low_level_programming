#include "main.h"
/***
 * put2 - function should print only one charcter out of two
 * starting with the first one
 * @str: input
 * Return: print
 */
void put2(char *str)
{
	int longin = 0;
	int t = 0;
	char *y = str;
	char *y = str;
	int o;
	{
		y++;
		longi++;
	}
	t = longi - 1;
	for (o = 0 ; o <= t ; o++)
	{
		if (o % 2 == 0)
		{
			_putchar(str[o]);
		}
	}
	_putchar('\n');
}
